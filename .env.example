# Токен бота от @BotFather в Telegram
BOT_TOKEN=your_bot_token_here

# ID группы, куда будут отправляться сообщения
# Чтобы получить ID группы:
# 1. Добавьте бота в группу
# 2. Отправьте любое сообщение в группу
# 3. Откройте в браузере: https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
# 4. Найдите "chat":{"id":-123456789} - это и есть GROUP_CHAT_ID
# Или используйте бота @userinfobot в группе
GROUP_CHAT_ID=your_group_chat_id_here

# Порт для сервера (по умолчанию 5001)
SERVER_PORT=5001

# Хост для сервера (по умолчанию 0.0.0.0)
SERVER_HOST=0.0.0.0

# Директория для загрузки файлов (по умолчанию uploads)
UPLOAD_FOLDER=uploads

# Firebase Cloud Messaging - путь к Service Account JSON файлу
# Получите в Firebase Console: Project Settings -> Service Accounts -> Generate New Private Key
# Сохраните файл как firebase-service-account.json в корне проекта
FCM_SERVICE_ACCOUNT_PATH=firebase-service-account.json